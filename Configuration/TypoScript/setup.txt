# ------------------------------------------------------------------------------
# Add CleverPush trackingcode
# ------------------------------------------------------------------------------
plugin.tx_cleverpush {
	settings {
		subdomain = {$plugin.tx_cleverpush.settings.subdomain}
	}
}
